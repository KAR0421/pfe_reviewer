// Edge: `if (x) return;` followed by an `else` branch. The return
// lives inside the then-branch's own block, so the else-branch — and
// the statement after the whole if — are reachable. SR021 must stay
// silent.
x := 1;
if (x = 1)
{
  return;
}
else
{
  msginfo("alive");
}
y := 2;
